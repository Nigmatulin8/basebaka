# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.pnpm-store
dist
dist-ssr
*.local

# Local secrets and credentials (examples: .env.example, google-oauth.example.json)
.env
.env.*
!.env.example

google-oauth.json
!google-oauth.example.json

basebaka.config.local.json

oauth-bundled.json

# Firebase / GCP key files (service accounts belong in ~/.basebaka/service-accounts)
credentials.json
*-firebase-adminsdk-*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Packaged Tauri sidecar (pnpm build:sidecar / ensure:sidecar)
src-tauri/binaries/*
